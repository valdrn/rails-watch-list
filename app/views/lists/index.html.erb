<h1>Lists</h1>
<ul class ="list-unstyled ml-2">
  <% @lists.each do |list| %>
    <li><%=link_to list.name, list_path(list.id)%></li>
  <% end %>
</ul>

<%= link_to "Add a new list", new_list_path, :class => "btn btn-primary ml-2" %>
