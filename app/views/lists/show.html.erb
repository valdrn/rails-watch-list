<h2><%= @list.name %></h2>

<ul class="list-group">
  <% @list.bookmarks.each do |bookmark| %>
    <li class="list-group-item"><%= bookmark.comment %><%= link_to "🔴 Delete", list_bookmark_path(bookmark.id), method: :delete, data: { confirm: "Are you sure?" }%></li>
  <% end %>
</ul>

<%= link_to 'Create a bookmark', new_list_bookmark_path(@list) %>
<%= link_to "Back to all lists", lists_path %>
